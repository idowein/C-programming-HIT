#define _CRT_SECURE_NO_WARNINGS

/*Libraries*/
#include <stdio.h>
#include <math.h>
#include <str.io>

int count(int* arr, int n) {
	int i = 0; int count = 0;
	for (i; i < n; i++) {
		if (arr[i]%2 == 0)
			count++;
	}
	return count;
}

void split(int* arr int n) {
	int size = sizeofint(int);
	int even = count(arr, n);
	int add = n - even;
	int evensize = int

}

void main() {

}